# gitleaks configuration for agents-opencode
# https://github.com/gitleaks/gitleaks

title = "agents-opencode gitleaks config"

[allowlist]
  description = "False positives and intentional test fixtures"

  [[allowlist.commits]]
    # commit: feat: add legal-advisor agent, skill, command, and reference files
    # false positive: generic-api-key matched on "encryption at rest/in transit, access controls"
    # fixed in 9753298 — historical commit still flagged
    description = "generic-api-key false positive in legal-advisor line 68"
    commits = ["dcdeef0483697932d8249d3f3232a2973591c912"]
